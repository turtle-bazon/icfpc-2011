Некоторые комбинаторы, реализованные нашими операциями

- вставить что-нибудь на следующий уровень дерева можно так:
  S(Ka)bc = Kac(bc) = a(bc), здесь a -- строка, b и c -- наши операции
  по сути, B-комбинатор
  как реализовать нашими операциями:
    собрать на слоте x строку a
    1 K x
    1 S x
    2 x b
    2 x c
    
- W x y = x y y
  W = SS(KI)
  в наших операциях:
    в t лежит I
    2 t S   -> S
    1 S t   -> S(S)
    1 K t   -> K(S(S))
    1 S t   -> S(K(S(S)))
    2 t K   -> S(K(S(S)))(K)
    2 t I   -> S(K(S(S)))(K)(I) = K(SS)I(KI) = SS(KI)
